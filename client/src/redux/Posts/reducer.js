import {types,actions} from './types';

console.log(types);
const initState={
};

export const postReducer=(state=initState,{type,payload})=>{
    switch(type){
        default :
            return state
    }
}